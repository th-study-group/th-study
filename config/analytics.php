<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Google Analytics 활성화
    |--------------------------------------------------------------------------
    |
    | 운영 환경에서만 true로 설정합니다.
    | 값이 누락되면 기본적으로 비활성화합니다.
    |
    */

    'enabled' => env('GA4_ENABLED', false),

    /*
    |--------------------------------------------------------------------------
    | Google Analytics 측정 ID
    |--------------------------------------------------------------------------
    */

    'measurement_id' => env('GA4_MEASUREMENT_ID'),

    /*
    |--------------------------------------------------------------------------
    | Google Analytics 허용 경로
    |--------------------------------------------------------------------------
    |
    | Laravel Request::is() 패턴을 사용합니다.
    |
    */

    'allowed_paths' => [
        '/',
        'blogs',
        'blogs/*/*/show',
        'portfolio',
        'posts/*',
        'posts/*/*/show',
        'intro',
    ],

];