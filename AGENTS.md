# AGENTS.md

## 0. 목적

이 문서는 TH-STUDY 프로젝트에서 AI 에이전트가 작업할 때 따라야 하는 최상위 규칙이다.

에이전트는 사용자의 요청을 받았다고 해서 바로 코드를 수정하지 않는다.

반드시 다음 순서로 작업한다.

1. 사용자 요청 분석
2. 관련 프로젝트 구조 및 기존 코드 확인
3. 관련 `agent_rules` 문서 확인
4. 필요한 경우 데이터베이스 구조 확인
5. 작업 목표 및 수정 범위 정의
6. 작업 계획 작성
7. 사용자 승인 대기
8. 사용자가 명시적으로 `진행`이라고 승인
9. 실제 코드 수정
10. 테스트 및 영향 범위 확인
11. 작업 결과 보고

---

# 1. 최우선 작업 원칙

## 반드시 계획을 먼저 작성한다

코드, 데이터베이스, 화면, 설정, 문서를 수정하기 전에 반드시 작업 계획을 먼저 작성한다.

계획에는 최소한 다음 내용을 포함한다.

- 작업 목표
- 확인한 관련 규칙
- 현재 구조
- 수정 대상 파일
- 데이터베이스 영향 여부
- 구현 방법
- 테스트 방법
- 예상 영향 범위

간단한 작업이라도 이 절차를 생략하지 않는다.

---

# 2. 사용자 승인 없이는 수정 금지

작업 계획을 작성한 뒤 반드시 사용자의 승인을 기다린다.

사용자가 명시적으로 아래 단어를 입력한 경우에만 실제 수정 작업을 시작한다.

`진행`

다음과 같은 표현은 승인으로 간주하지 않는다.

- 만들어봐
- 추가해줘
- 수정해줘
- 해봐
- 적용해줘
- 구현해줘
- 바꿔줘
- 해줘
- ㄱㄱ
- 오케이
- 좋아
- 알겠어

위 표현이 포함되어 있어도 먼저 작업 계획을 작성하고 승인을 기다린다.

사용자가 정확하게 `진행`이라고 승인하기 전에는 파일 생성, 수정, 삭제 및 데이터베이스 변경을 하지 않는다.

---

# 3. 승인 전 허용되는 작업

사용자의 `진행` 승인 전에는 읽기와 분석만 수행한다.

허용:

- 파일 목록 확인
- 디렉터리 구조 확인
- 기존 코드 읽기
- `AGENTS.md` 읽기
- `agent_rules/*.md` 읽기
- Migration 읽기
- 설정 파일 읽기
- Route 확인
- Controller / Service / Repository 구조 확인
- Blade / JavaScript / CSS 구조 확인
- 기존 테스트 확인
- 검색 명령 실행
- Git 변경 상태 확인
- 작업 계획 작성

금지:

- 파일 생성
- 파일 수정
- 파일 삭제
- 코드 자동 수정
- Migration 생성
- DB Schema 변경
- 패키지 설치
- Composer/NPM 의존성 변경
- Git commit
- Git push
- 기존 데이터를 변경하는 명령 실행

---

# 4. 규칙 문서 위치

프로젝트별 세부 개발 규칙은 다음 디렉터리에 존재한다.

`agent_rules/`

현재 규칙 문서:

- `agent_rules/backend.md`
- `agent_rules/frontend.md`
- `agent_rules/board.md`
- `agent_rules/note.md`
- `agent_rules/mcp.md`
- `agent_rules/database_schema.md`

작업을 시작하기 전에 요청과 관련된 규칙 문서를 반드시 확인한다.

---

# 5. 작업별 규칙 적용

## 백엔드 작업

다음 작업이 포함되면 반드시 아래 문서를 확인한다.

`agent_rules/backend.md`

대상 예시:

- Controller
- Service
- Repository
- Model
- FormRequest
- Policy
- Middleware
- Route
- API
- 인증/인가
- 서버 로직
- CRUD
- Validation

---

## 프론트엔드 작업

다음 작업이 포함되면 반드시 아래 문서를 확인한다.

`agent_rules/frontend.md`

대상 예시:

- Blade
- HTML
- CSS
- Bootstrap
- JavaScript
- 화면 구성
- UI/UX
- 반응형
- 목록 화면
- 등록/수정 화면
- Modal

---

## 게시판 작업

게시판 관련 작업이면 반드시 아래 문서를 확인한다.

`agent_rules/board.md`

게시판 작업에 백엔드 또는 프론트엔드 변경이 포함되면 관련 문서를 함께 확인한다.

예:

`agent_rules/board.md`
`agent_rules/backend.md`
`agent_rules/frontend.md`

---

## 노트 작업

노트 관련 작업이면 반드시 아래 문서를 확인한다.

`agent_rules/note.md`

노트 작업에 백엔드 또는 프론트엔드 변경이 포함되면 관련 문서를 함께 확인한다.

예:

`agent_rules/note.md`
`agent_rules/backend.md`
`agent_rules/frontend.md`

## MCP 작업

MCP Tool, MCP API 및 MCP 연동과 관련된 작업이면 반드시 아래 문서를 확인한다.

`agent_rules/mcp.md`

MCP Backend 변경이 포함되면 다음 문서를 함께 확인한다.

`agent_rules/backend.md`

Database 조회 또는 변경이 포함되면 다음 문서를 함께 확인한다.

`agent_rules/database_schema.md`

화면 변경이 포함되면 다음 문서를 함께 확인한다.

`agent_rules/frontend.md`

게시판 또는 노트 등 특정 기능의 MCP 작업이면 해당 기능 규칙도 함께 확인한다.

예:

게시판 MCP Tool:

- `agent_rules/mcp.md`
- `agent_rules/backend.md`
- `agent_rules/board.md`
- `agent_rules/database_schema.md`

노트 MCP Tool:

- `agent_rules/mcp.md`
- `agent_rules/backend.md`
- `agent_rules/note.md`
- `agent_rules/database_schema.md`

---

# 6. 데이터베이스 규칙

데이터베이스 관련 작업에서는
`agent_rules/database_schema.md`를 참조한다.

단, 작업과 관련 없는 전체 스키마를 불필요하게 모두 읽지 않는다.

먼저 테이블 요약 및 관계 정보를 확인하고,
현재 작업과 관련된 테이블만 검색하여 상세 컬럼을 확인한다.

예:
- 회원 기능 → users 및 관련 인증 테이블
- 게시판 기능 → posts, comments, post_histories
- 노트 기능 → note_* 관련 테이블
- 유입 통계 → access_logs, bot_access_logs, daily_page_stats, conversion_logs

신규 테이블 또는 컬럼이 필요한 경우에는
관련 기존 테이블과 관계를 먼저 확인한 뒤 설계한다.

스키마 문서와 실제 구현이 충돌하면 다음 순위를 따른다.

1. 실제 Migration
2. 현재 프로젝트 코드
3. `agent_rules/database_schema.md`

---

# 7. 데이터베이스 판단 기준

`agent_rules/database_schema.md`는 프로젝트 데이터베이스 구조를 이해하기 위한 참조 문서다.

다만 실제 구현과 문서가 충돌하는 경우 다음 우선순위를 따른다.

1. 실제 Migration
2. 현재 프로젝트 코드
3. `agent_rules/database_schema.md`

문서와 실제 코드가 다른 경우 임의로 수정하지 않는다.

차이를 사용자에게 보고하고 작업 계획에 포함한다.

---

# 8. 기존 프로젝트 구조 우선

새로운 기능을 구현할 때 에이전트가 선호하는 구조를 임의로 도입하지 않는다.

반드시 기존 프로젝트의 구현 방식을 먼저 조사한다.

예를 들어 CRUD 기능을 추가한다면 기존 CRUD 기능에서 다음 구조를 먼저 확인한다.

- Route
- Controller
- FormRequest
- Service
- Repository
- Model
- Blade
- JavaScript
- CSS
- Validation
- 권한 처리
- 로그 처리
- 테스트

기존 프로젝트에 동일하거나 유사한 구현이 있다면 해당 패턴을 우선 사용한다.

---

# 9. 추측 구현 금지

확인하지 않은 내용을 사실처럼 가정하지 않는다.

예:

- 존재하지 않는 테이블을 있다고 가정
- 존재하지 않는 컬럼을 사용
- 존재하지 않는 Service 호출
- 존재하지 않는 Route 사용
- 존재하지 않는 CSS 클래스 사용
- 프로젝트에 없는 라이브러리를 사용
- 기존 구조를 확인하지 않고 새로운 아키텍처 도입

모르는 부분이 있다면 먼저 프로젝트에서 검색한다.

검색해도 확인할 수 없는 경우 작업 계획에서 명확하게 표시한다.

---

# 10. 신규 기능 작업 절차

신규 기능 요청을 받으면 다음 순서로 분석한다.

1. `AGENTS.md` 확인
2. 관련 `agent_rules/*.md` 확인
3. MCP 관련 작업이면 `agent_rules/mcp.md` 및 기존 유사 MCP Tool 확인
4. 기존 유사 기능 검색
5. Route 확인
6. Controller 구조 확인
7. Service / Repository 구조 확인
8. Model 확인
9. 관련 Blade 및 프론트 구조 확인
10. `agent_rules/database_schema.md` 확인
11. 관련 Migration 확인
12. 재사용 가능한 기존 구조 확인
13. 신규 구현이 필요한 부분 정의
14. 작업 계획 작성
15. 사용자 승인 대기

이 단계에서는 실제 코드를 수정하지 않는다.

---

# 11. 작업 계획 출력 형식

실제 작업 전에는 가능한 한 다음 형식으로 사용자에게 보고한다.

## 작업 목표

요청한 기능과 최종적으로 구현할 내용을 설명한다.

## 확인한 규칙

이번 작업에 적용한 규칙 파일을 작성한다.

예:

- `agent_rules/backend.md`
- `agent_rules/frontend.md`
- `agent_rules/database_schema.md`

## 현재 구조

관련 기존 코드와 데이터베이스 구조를 간단하게 설명한다.

## 수정 대상

수정하거나 생성할 가능성이 있는 파일을 작성한다.

## 구현 계획

1. 첫 번째 작업
2. 두 번째 작업
3. 세 번째 작업

## 데이터베이스 영향

- 없음

또는

- Migration 추가 예정
- 신규 테이블 추가 예정
- 기존 컬럼 변경 예정

## 테스트 계획

구현 후 어떤 방법으로 검증할지 설명한다.

마지막에는 반드시 다음과 같이 사용자 승인을 요청한다.

`진행`이라고 입력하면 위 계획대로 작업하겠습니다.

---

# 12. 승인 후 작업

사용자가 `진행`이라고 입력하면 계획에서 승인받은 범위만 수정한다.

승인 이후에도 예상하지 못한 큰 구조 변경이 필요한 경우 임의로 작업 범위를 확대하지 않는다.

예:

- 새로운 테이블이 추가로 필요함
- 기존 컬럼 삭제가 필요함
- 패키지 설치가 필요함
- 인증 구조 변경이 필요함
- 공통 모듈 대규모 변경이 필요함
- 기존 API 호환성이 깨짐

이 경우 작업을 멈추고 변경 이유와 새로운 계획을 사용자에게 보고한다.

추가 승인을 받은 뒤 계속한다.

---

# 13. 작업 완료 후 검증

코드 수정 후 가능한 범위에서 반드시 검증한다.

예:

- 문법 오류 확인
- Route 확인
- PHP/Laravel 테스트
- 관련 테스트 실행
- 변경 파일 확인
- Git diff 확인
- 기존 기능 영향 확인

테스트가 실패한 경우 성공한 것처럼 보고하지 않는다.

실행하지 못한 테스트가 있다면 그 사실을 명확하게 작성한다.

---

# 14. 작업 완료 보고

작업이 끝나면 다음 내용을 간단하게 보고한다.

## 완료 내용

실제로 변경한 내용을 설명한다.

## 변경 파일

변경된 파일을 정리한다.

## 데이터베이스 변경

DB 변경 여부와 Migration 내용을 설명한다.

## 테스트 결과

실행한 테스트와 결과를 설명한다.

## 참고사항

추가 확인이 필요한 내용이 있을 경우 작성한다.

---

# 15. 금지 사항

다음 행동을 하지 않는다.

- 승인 전에 코드 수정
- 기존 구조를 확인하지 않고 신규 구조 생성
- 데이터베이스 구조 추측
- 존재하지 않는 파일/클래스/컬럼을 있다고 가정
- 요청하지 않은 대규모 리팩터링
- 관련 없는 파일 수정
- 기존 기능을 임의로 삭제
- 테스트 실패를 숨김
- 사용자 승인 범위를 넘어선 변경
- 사용자 요청 없이 Git commit 또는 push
- 사용자 요청 없이 운영 데이터 변경

---

# 16. 핵심 원칙

항상 다음 순서를 지킨다.

**읽기 → 분석 → 규칙 확인 → 기존 구조 확인 → 계획 → 승인 → 진행 → 검증 → 보고**

사용자가 기능을 만들어 달라고 요청해도 바로 구현하지 않는다.

먼저 프로젝트와 규칙을 확인하고 작업 계획을 제시한다.

사용자가 명시적으로 `진행`이라고 승인한 이후에만 실제 파일을 수정한다.

---

## Migration과 스키마 문서 동기화

Migration으로 데이터베이스 구조를 추가·변경·삭제하는 경우,
Codex는 Migration 변경과 함께 `agent_rules/database_schema.md`도 갱신한다.

다음 항목의 변경은 모두 스키마 문서 갱신 대상이다.

- 테이블 추가·변경·삭제
- 컬럼 추가·변경·삭제
- Primary Key 변경
- Foreign Key 및 관계 변경
- 일반 인덱스 변경
- Unique 인덱스 변경
- 복합 인덱스 변경

Migration과 스키마 문서가 불일치하면 실제 Migration을 기준으로 스키마 문서를 수정한다.
문서 갱신 후에는 Migration과 스키마 문서의 일치 여부를 함께 검증한다.
